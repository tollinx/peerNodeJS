// Need environment variables
const PRIVATE_KEY = 1234

// Sender address
// const senderAddress = 

// Recipient address
//

// const transaction = {
//     from: sender
//     to: recipient
//     value: 
//     price: 

// }

async function sendTransaction() {
    try {
        // Retrieve sender confirmation?

        // Create transaction object with params

        // Sign the transaction with private key

        // Send the transaction with await
    } catch (error) {
        console.error('Error sending transaction: ', error);
    }
}

async function requestTransaction() {
    try {
        // Wait for the transaction with await

        verifyTransaction()
    } catch (error) {
        console.error('Error requesting transaction: ', error);
    }
}

async function verifyTransaction() {
    try {
        // Use some sort of key or hash to verify the transaction
        //
    } catch (error) {
        console.error('Error verifying transaction: ', error);
    }
}
